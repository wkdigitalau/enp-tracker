server {
    listen 80;
    server_name elitenursepartners.com.au www.elitenursepartners.com.au;

    root /opt/enp/dist/public/enp-review-28aug26;
    index index.html;

    location ^~ /.well-known/acme-challenge/ {
        allow all;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
